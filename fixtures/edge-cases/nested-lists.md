# Nested

- Top-level note about the project
  - NEVER commit `.env`
  - NEVER edit `/secrets`
- Other note
  - ALWAYS run `lint` before `push`

A paragraph that is not a rule.

> NEVER commit `id_rsa`

Inline `code` with no rule.
