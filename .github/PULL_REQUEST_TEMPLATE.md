<!-- One-line summary. Linked issue: Fixes #123 -->

## What changed

-

## Why

-

## Checklist

- [ ] `npm run verify` is green locally (lint + typecheck + test + build).
- [ ] If a hook template changed, `wisp-rulecast verify` still denies every synthetic violation in the fixtures.
- [ ] If a rule kind was added, both `docs/rule-grammar.md` and `docs/hook-templates.md` reflect it.
- [ ] CHANGELOG entry under `[Unreleased]` (omit for `chore(deps)` and `docs` only changes).
